# Used when you run `cd frontend && npm run build` locally.
# On Vercel, set the same key in the dashboard (Environment Variables).
# Replace with your real public API URL before a production build.
VITE_API_URL=https://YOUR-BACKEND-HOST/api
