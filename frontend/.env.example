# Frontend — local dev (Vite proxies /api → localhost:5000)
VITE_API_URL=http://localhost:5000/api

# Production — set at build time (nginx / Cloudflare Pages / etc.)
# VITE_API_URL=https://api.your-domain.example/api
