# Frontend — local dev (Vite proxies /api → localhost:5000)
VITE_API_URL=http://localhost:5000/api

# Production — set in Vercel Dashboard → Environment Variables (not in this file)
# VITE_API_URL=https://your-api-host.example.com/api
